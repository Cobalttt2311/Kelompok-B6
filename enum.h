#ifndef enum_h
#define enum_h
#include <stdio.h>
#include <stdlib.h>

// enum KeySize, digunakan untuk merepresentasikan ukuran kunci
enum keySize;
//enum errorCode, untuk penanda kesalahan
enum errorCode;

#endif
